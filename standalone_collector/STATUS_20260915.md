# App-owned Model 1 collector — actual checkpoint (2026-09-15)

## Implemented and deployed

A separate Render web service was created in the already approved workspace:
- service: decision-hub-model1-collector
- ID: srv-dakhacou01pc73f58aug
- branch: app/model1-standalone-20260915
- last deployed application commit: 7a220b313701a9ea89b9fee2db26b5819ddb9b40
- last deployment: dep-dakhdkmk1f9s73darbrg (live)
- plan: free, for source feasibility testing; automatic deploy is OFF.

The service entrypoint is standalone_collector/app.py, NOT main.py or ai_candidate_main.py. prepare.py copies and hashes the two pinned legacy source and OpenAI modules into an app-owned runtime. Only that runtime copy receives a bounded read-only chart readiness check and passive diagnostics. The original bot files and existing services were not changed. No Telegram/trading/research process is started.

The 10 source-readiness/unit checks passed in the Render build. These are synthetic checks, not proof that the real chart is readable.

## Actual live-source findings

Five browser capture attempts were made in this work session: three in the temporary Lovable diagnostic environment, two in the independent Render build environment. No OpenAI inference or Firecrawl call was made. A separate image-only crop review made no source call.

BTC / Model 1 / Symbol / 12 hour could be selected. However, the chart remained unreadable after the bounded 60-second final wait in both environments. Native-pixel review confirmed genuinely blurred axis labels. The final instrumented Lovable browser showed a visible 40x40 role=progressbar overlapping the chart, with visible ancestors and an animated inner indicator; this was not just a tiny-image preview or hidden-element false positive.

Render passive diagnostics showed no HTTP >=400 responses and no page errors. One Google Analytics request was cancelled; that is not evidence of a CoinGlass data failure. In the final Lovable diagnostic, 10 normal page-generated CoinGlass JSON responses were inspected only for top-level diagnostic fields: all HTTP200; three success:false/code:40000 without a message, seven successful. We did not replay endpoints or inspect encrypted/hidden market payloads. This code cannot be attributed with certainty to the chart request or to authentication.

There was no visible login wall, challenge, or paywall message in those observations. Root cause remains unresolved. Do not claim that login is required, that a longer wait fixed it, or that source access is proven working.

## Disabled / not completed

COLLECTION_BRIDGE_ENABLED=false. No OpenAI key, dedicated bridge token or database connection was configured for this new service. No model inference, worksheet write, frontend publication or hourly source schedule occurred.

The existing app-side bridge is still present in trade-plan-pad; it has not been connected to this new origin. Before configuration and any publication:
1. Compare a currently readable user-browser Model1/BTC/12H page with the failing clean source capture. Do not ask for passwords or cookies in chat.
2. Resolve legitimate chart readiness/access and verify a clear image with readable current price/axes.
3. Configure OpenAI through the secure key flow, dedicated bridge authentication, and persistent result storage; complete isolated database tests.
4. Run one real capture/analysis/result-persistence/app-save/reload test as an existing authorized editor.

The build currently includes --probe (one source-only attempt per explicitly triggered build), not a recurring job. Avoid unnecessary redeploys; updating Render environment variables triggers a deploy. Remove the diagnostic probe from the deployment path or gate it before routine configuration/deployment. Public diagnostic image serves ONLY an unauthenticated public-page capture at a fixed path while PUBLIC_SOURCE_DIAGNOSTIC=true; disable it after troubleshooting.

Lovable reported 8.6 credits for this session's source diagnosis and saved-image inspection. Zero OpenAI inference calls is not a claim that the entire engineering work was free. Do not purchase a paid instance merely to fix this unresolved source issue without evidence and user approval.
