# נקודת המשך — קליטת כל סריקות Watch למחקר

נבדק ב־13.09.2026 בשעה 18:42:14 UTC, כלומר 21:42:14 בישראל.

## החלטת המשתמש וסדר העבודה

המשתמש אישר להתחיל, צעד אחד בכל פעם: מחקר מכל הסריקות; גילוי נוסחאות חדשות באופן שוטף; השלמת הארכיון ההיסטורי; השלמת שילוב HYPE; אימות נוסחאות על מקרים חדשים.

אישור זה מחליף את האמירה הקודמת שאין עוד פיתוח לביצוע. הושלם כעת **רק צעד המימוש הראשון בנושא הראשון: קליטה והנגשה של הסריקות הקפואות למחקר**. טרם הושלם המחקר מכל הסריקות, ואין לעבור לנושא הבא כאילו הושלם.

מחקר השערות על רכיבי אשכולות ו־CVD, שלב B4 ונרמול משפחת 240m עדיין נדחים. לא שונו ציונים או תנאי נוסחאות; לא הופעל Watch ידנית ולא נשלחה הודעת Telegram לבדיקה.

## מה הותקן

- PR [33](https://github.com/hadarkamer/crypto-intelligence-platform/pull/33) מוזג ל־main: `43ca09b964d04adae95c06be02a0fa83d0c4ba17`.
- העץ שנבדק: `6caaecc66937384390de33b095b8dbfaa3acc2b5`. ריצת CI `34774846545` עברה בכל 109 קובצי ה־selftest, כולל 11 הבדיקות החדשות ובהן 5 בדיקות PostgreSQL מבודדות.
- מיגרציה `046_watch_scan_research_intake.sql` הוחלה. פריסת Render `dep-dajeqjdckfvc739p18c0` עלתה ב־18:38:29 UTC; מופע `z84kj`.
- המימוש ב־`research_watch_scan_intake.py`, בדיקותיו ב־`research_watch_scan_intake_selftest.py`; הפעלה, עצירה ובריאות משולבות ב־`main.py`, והמיגרציה רשומה ב־`research_formula_schema_admin.py`.

הצרכן `watch-all-scan-intake-v1` קורא את הבלוק הקיים `source_metadata.capture_metadata.operational_scores` בטבלת `research_max_pain_snapshot_sets`. הוא מקבל רק `watch-operational-scores-v2` עם hash תקין וזהות מקור תואמת. הוא אינו מחשב מחדש ציונים.

אוכלוסיית המחקר הנפרדת היא `watch-all-scan-observations-v1`: שמונה מטבעות, שבעה אופקים ושני צדי חיסול — 112 תאים לסריקה. התצוגות `research_watch_scan_observations` ו־`research_watch_scan_score_slots` מפנות למקור הבלתי משתנה; הבלוק אינו מועתק מחדש למסד. קבלות הקליטה וה־cursor נשמרים יחד. סריקת מזהים מחזורית מוגבלת וקצבה קטנה לנתונים האחרונים מאפשרות התאוששות מאתחול ומטרנזקציה עם מזהה נמוך שהתחייבה באיחור.

## מה אומת בפועל

| בדיקה | תוצאה |
|---|---:|
| סריקות v2 שהתקבלו | 15 |
| סריקות v1 שנדחו במפורש | 1 — `UNSUPPORTED_CAPTURE_VERSION` |
| סריקות קיימות שממתינות לקבלת קליטה | 0 |
| תצפיות מטבע | 120 |
| תאי מחקר | 1,680 |
| ציונים מתחת ל־65 | 1,594 |
| ציוני אפס | 226 |
| תאי HYPE | 210 |
| סריקות ללא שום ציון 65 ומעלה | 5 |

לא נמצאו כיסוי שגוי, מפתחות כפולים, חריגות זמן או כיוון, דגלי כשירות בלתי צפויים או שדות מקור נדרשים חסרים בבדיקות שבוצעו. נתוני HYPE שומרים את זהות מקור המחיר שלהם; אין בכך טענה ששילוב תוצאות HYPE הושלם.

כל הסריקות שנצפו בבדיקה זו מסומנות `HISTORICAL_CAPTURE`. טרם נצפתה קליטת `FORWARD_CAPTURE` אחרי ההפעלה. כל התאים מסומנים `NOT_EVALUATED`; אין בשלב זה חישוב תוצאות לכל סריקה או הערכת נוסחאות על אוכלוסייה זו. גם קליטה עתידית אינה ראיה פרוספקטיבית לנוסחה בלי הקפאה נפרדת של אותה נוסחה מראש.

זמן התצפית הוא `computed_at_utc`; `usable_from_utc` הוא המאוחר מבין זמן זה וזמני `available_at_utc` ו־`created_at_utc` של המקור. הוא אינו מוגדר כמחיר כניסה או כהחלטת מסחר. ציוני אפס, יעד לא פעיל, נתון חסר ומודל בלתי זמין נשמרים כמצבים שונים. כיוון המחקר הפוך פעם אחת מצד החיסול.

פירוט מכני: [2026-09-13-all-scan-intake-verification.json](2026-09-13-all-scan-intake-verification.json).

## הצעד הבא שכבר אושר

להמשיך **בתוך נושא המחקר מכל הסריקות**: לבדוק תחילה את חוזי המחיר, התוצאות וגלי BTC הקיימים, ואז לבנות מתאם גרסתי שמקשר תצפיות למחירים, לתוצאות ולגלי שוק בלי שימוש במידע שטרם היה זמין בזמן הרלוונטי. לשמור את האוכלוסייה החדשה נפרדת מאוכלוסיית ההתראות שנמסרו, ולהשתמש בנתיבי המחיר המפורשים בלי ערבוב מקורות.

לאחר השלמת נושא זה, סדר הנושאים המאושר הוא: גילוי נוסחאות חדשות שוטף → ארכיון היסטורי → השלמת HYPE → אימות נוסחאות על מקרים חדשים. להתקדם צעד אחד, לאמת ולדווח; אין להסיק מהקליטה הנוכחית שכבר חושבו תוצאות או התגלו נוסחאות.
