# Captured question search, 7 September 2026

The recurring ordered-v7 worker now evaluates a finite prespecified catalog
against immutable delivered LIVE alert snapshots. The seven existing total65
definitions are unchanged. `candidate_catalog(include_extended=True)` adds
100 normal definitions and 107 separately identified inverses (214 total).

The additional predicates cover the original selected/opposite MaxPain averages
(55/60/65/70/75/80 and intermediate bands), exact component fields, selected
minus opposite score, valid consensus numerator and denominator, selected TF
score comparisons, captured confirmation states, Combined, valid liquidity
bands, the existing total65 families paired with liquidity, Spot/Futures
contradiction, a justified gap/proximity/consensus conjunction and a four-family
triple65+liquidity condition. The causal past-price sidecar adds exact prior
Spot return signs at15m/30m/1h/4h/12h/24h, same-cutoff BTC1h sign and relative
strength, and Spot65 with/against its completed prior1h move. FLAT means an
exactly zero return; it is not an invented sideways-range threshold.
Conditions are never chosen from outcomes.

`docs/ordered_research_question_map.json` preserves all Q01–Q72 question text,
justifications and required fields. It does not replace the user's Sheet.
Migration030 adds durable input fingerprints, question coverage/failure trials
and per-scope input identities. Feature-only work is explicitly
`PARTIAL_FEATURE_SCREEN`, not statistical testing. Real descriptive outcome
cells are recorded separately as `PARTIAL_ORDERED_V7_DESCRIPTIVE` or blocked
evidence. The statuses never advertise acceptance of an incomplete question.

The worker fetches source IDs first, projects only relevant captured JSON, and
checks new or changed feature fingerprints. Unchanged input is skipped. At most
eight new candidate/symbol/direction/period cells are registered per pass, each
with all eight thresholds and four independent horizons. Existing and new
cells rotate by last checked time; identical outcomes and FRESH membership do
not repeat evaluation. Failed and zero-match conditions remain in question
trials rather than expanding the queue into thousands of empty cells.

Liquidity support means a source-validated MaxPain selected-target side: the
captured near/far amounts must be nonnegative, their sum positive, and the
captured selected share must match their ratio within displayed precision.
The source side, native analysis direction and actual target direction must
agree. Combined timeframe percentages are not silently treated as this amount
contract. Missing and inconsistent fields stay coverage diagnostics, never
zero or no-signal baselines. The live writer's original
`calculation_validation_errors` also invalidates the source-direction features.

Normal and inverse matches retain the original source event ID and BTC parent
cohort. Inverse measurement references a separately materialized canonical
research decision sample and its actual ordered-v7 labels. Predicates continue
to use the original features; only the measurement direction changes once.
Missing inverse outcomes remain DATA_MISSING. No synthetic delivery or new
independent market wave is created.

Both research periods remain LIVE-only and exclude whole BTC parents that
cross their respective cutoff. FRESH remains a separate rolling14-day route.
Common-window metrics and immutable prospective freezes are integrated through
the sidecar and validation modules. Sheet median fields contain actual
common-window medians only. The asymmetry ratio uses equal-weight wave cohorts
with conservative min-MFE/max-MAE across simultaneous members. Historical
stop-at-first-touch metrics remain distinguishable audit values in result JSON.

There is no fabricated acceptance threshold, ranking weight, prior-price
history or range-regime classification. Sequence/repetition, past-candle range
regimes, full neutral comparisons and unexported GOAT averages retain named
missing pipeline steps. Missing sidecar windows never become usable prior
returns. Q07 never recreates the old unbound 66.2% claim.
Archive evidence stays in its separate source/entry contract.
